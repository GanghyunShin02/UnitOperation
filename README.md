# UnitOperation

This repository contains codes developed for Unit Operation experiments.

Experimental data are stored in Excel files, and all calculations are carried out using Jupyter Notebooks.

Each directory includes:
- Jupyter Notebook files (`.ipynb`)
- Excel data files (`.xlsx`)
- Additional experimental materials (if available)
